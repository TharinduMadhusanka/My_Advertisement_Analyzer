import React from "react"; // Make sure to import React for functional components
import MainLayout from "../layout/mainLayout";

function Home() {
  return (
    <MainLayout>
      <h1>Home</h1>
    </MainLayout>
  );
}

export default Home; // Export the Home component so it can be used in other files
